{
  "description": "Adopts a Unity Hub-started editor for this checkout on session start, when this project has been prepared for the Unity MCP bridge (scripts/prepare-unity-worktree.ps1 has generated .seretos/unity-mcp-launch.ps1). Harmless, silent no-op on any other project - Claude Code only, see skills/unity-wrapper/SKILL.md's Unity Hub adoption section for the Codex equivalent.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -File \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start-adopt.ps1\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
